# Segredos reais - nunca commitar (fiquem só em .env.example com placeholders)
.env
*.env
!.env.example

# Backups locais de scripts (podem conter valores hardcoded de sessões de debug)
*-bkp.ps1
*.bak

# Certificado digital A1 e documentos societarios - NUNCA commitar
# (certificado-A1.pfx e uma chave privada de assinatura digital; CNPJ.pdf
# e documento societario. Ambos ficam so localmente, montados/copiados no
# build local, jamais no repositorio publico.)
certs/

# Cruft de SO
.DS_Store
Thumbs.db
